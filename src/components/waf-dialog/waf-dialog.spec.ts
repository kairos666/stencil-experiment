import { WafDialog } from './waf-dialog';

describe('waf-dialog', () => {
    it('should build', () => {
        // test component class build
        expect(new WafDialog()).toBeTruthy();
    });
});